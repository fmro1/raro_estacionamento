import 'package:flutter/material.dart';

class RemoveVehicleView extends StatelessWidget {
  const RemoveVehicleView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
